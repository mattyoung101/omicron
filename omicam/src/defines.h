#pragma once

// Misc constants and settings
// TODO put these into an INI!!!

#define VERBOSE_LOGGING // whether or not verbose logging is enabled (LOG_TRACE if true, otherwise LOG_INFO)
//#define ENABLE_DIAGNOSTICS // whether or not diagnostics are enabled (verbose logging should be enabled too)